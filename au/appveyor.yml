# AU template: https://github.com/majkinetor/au-packages-template

version: '{build}'
max_jobs: 1
image: WMF 5
clone_depth: 5
branches:
  only:
  - master
#build:
#  verbosity: minimal

environment:
  # Set au version to use or omit to use the latest. Specify branch name to use development version from Github
  au_version:
  au_push: true

  # Github token to commit pushed packages to repository
  github_user_repo: jtcmedia/chocolatey-packages
  github_api_key:
    secure: BlX06UEvY8mIugdlfSMO1micKLzEh3yD46GLv/r5nu4afIep2cUvK3CyB0d/RIh8


  # Mail credentials - for error notifications
  mail_user:
    secure: xIymlMr2HncMoUGsMN3zVg40ydOmn0ybrYF/qYT7Npg=
  mail_pass:
    secure: u06lR/WFO1C39W5cfPL7kw==
  mail_server: smtp.gmail.com
  mail_port: 587
  mail_enablessl: true

  # ID of the gist used to save run results - create a gist under the github_user (secret or not) and grab the id - https://gist.github.com/name/id
  # Optional, leave empty to create anonymous gist
  gist_id: 8abc37a8c3b020ffdd9704a810331790

  # Chocolatey API key - to push updated packages
  api_key:
    secure: C4/A+I8yif2gUHRYW908lGi1HopJsgIQlA0TDFEaCaFXqimsFbs1DOANcYSwipEu

init:
- git config --global user.email "chocolatey@realdimensions.net"
- git config --global user.name  "Chocolatey"
- git config --global core.safecrlf false

install:
- ps: 'Get-CimInstance win32_operatingsystem -Property Caption, OSArchitecture, Version | fl Caption, OSArchitecture, Version'
- ps: $PSVersionTable
- ps: |
    $x=$null; $is_branch = ($Env:au_version -ne $null) -and ([version]::TryParse($Env:au_version, [ref]$x) -eq $false)
    if ($is_branch) {
        pushd ..
            git clone -q https://github.com/majkinetor/au.git
            cd au
            git fetch
            git checkout -q $Env:au_version
            ./build.ps1 -Install -NoChocoPackage
        popd
    } else {
        Install-PackageProvider -Name NuGet -Force
        Set-PSRepository -Name PSGallery -InstallationPolicy Trusted

        $params = @{ Name = 'au' }
        if ( $Env:au_version -ne $null ) { $params.MinimumVersion = $Env:au_version }
        Install-Module @params

        Get-Module au -ListAvailable | select Name, Version
    }

build_script:
- ps: |
    ./au/update_all.ps1

#on_finish:
#- ps: $blockRdp = $true; iex ((new-object net.webclient).DownloadString('https://raw.githubusercontent.com/appveyor/ci/master/scripts/enable-rdp.ps1'))

artifacts:
- path: au/update_info.xml
- path: au/Update-AUPackages.md

notifications:
- provider: Email
  to: $(mail_user)
  on_build_success: false
  on_build_failure: true
  on_build_status_changed: true

